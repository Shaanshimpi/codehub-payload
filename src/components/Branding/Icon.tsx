import React from 'react'
import { CustomComponent } from 'payload'

const Icon: CustomComponent = (props) => {
  return <div style={{ fontWeight: 'bold', fontSize: '1.5rem' }}>🔥</div> // or your SVG/icon
}

export default Icon
